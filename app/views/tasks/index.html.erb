<h1>Tasks</h1>

<h3><%= link_to "Add a new tasks", tasks_new_path %></h3>

<% @tasks.each do |task| %>
  <h2><%= link_to task.title, task_path(task.id) %></h2>
  <h2><%= link_to "+ New Task", tasks_new_path %></h2>
  <p>Completed: <strong><%= task.complete %></strong></p>
<% end %>

